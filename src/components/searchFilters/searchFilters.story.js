import React from 'react';
import { storiesOf } from '@storybook/react';
import { SearchFilters } from './searchFilters';

storiesOf('SearchFilters', module).add('SearchFilters', () => (
  <SearchFilters />
));
