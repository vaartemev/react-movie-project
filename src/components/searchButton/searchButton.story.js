import React from 'react';
import { storiesOf } from '@storybook/react';
import { SearchButton } from './searchButton';

storiesOf('SearchButton', module).add('SearchButton', () => <SearchButton />);
